import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        marginTop: 25,
        marginBottom: 100,
    },
    title:{
        color: '#64748B',
        fontSize: 30,
        textAlign: 'center',
        fontWeight: '700',
        marginTop: 20,
    },
    noExpenses:{
        marginVertical: 20,
        textAlign: 'center',
        fontSize: 20,
    },
})

export default styles